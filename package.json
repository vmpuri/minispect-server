{
  "name": "minispect-server",
  "version": "1.0.0",
  "description": "Server-side processing for Minispect App, i.e. retrieving models from remote server, data transmission to backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
